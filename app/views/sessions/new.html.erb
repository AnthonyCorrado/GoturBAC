<h1><center>Sign In, Drunky!</center></h1>

    <%= form_for :session, :url => sessions_path do |f| %>

    <div class= "input_fields">

<h3>Name</h3>
        <%= f.text_field :name %>

<h3>Password</h3>
        <%= f.password_field :password %>

    </div>

    <div class= "input_buttons">
        <%= f.submit "Get Drunk!", class: "large button expand radius round [secondary success alert]" %><br>

        <%= link_to "Sign Up For Madness", new_user_path, class: "large button expand radius round [secondary success alert]" %>
    </div>

<!-- Global variable used so user start time can be set over on user controller. Global variable is ok to use in this case, since once the session is created the value is stored as a user attribute so it remains unique to the user session. -->

<% end %>

<% $starttime = Time.now %>