
<!-- Jonnys car app might have good code. Also try searching for adding from collection selects, or maybe try rearranging the collection attributes (just make sure to save this current collection_select somewhere first) -->
<%= form_for [@user, @drink] do |f| %>

Beers: <%= collection_select :user_drinks, :drink_ids, Drink.all, :id, :name %>

<%= f.submit %>

<% end %>